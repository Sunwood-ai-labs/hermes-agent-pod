#!/usr/bin/env bash
set -euo pipefail

script_path="$(python3 -c 'import os, sys; print(os.path.realpath(sys.argv[1]))' "$0")"
script_dir="$(dirname "$script_path")"
exec python3 "$script_dir/hermes-worker.py" "$@"
